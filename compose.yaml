services:
  onehost:
    image: ghcr.io/mynamesishuman/onehost:latest
    container_name: onehost
    restart: unless-stopped
    command: app --token=${APP_TOKEN}
    ports:
      - "4444:4444"
    networks:
      private:
        ipv4_address: 10.0.0.10

networks:
  public:
    driver: bridge
  private:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.0.0/24
          gateway: 10.0.0.1